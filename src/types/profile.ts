// types/profile.ts
interface CompanyProfile {
    name: string;
    code: string;
    taxId: string;
    address: string;
    phone: string;
    email: string;
    defaultCurrency: string;
   }
   
   interface UserProfile {
    name: string;
    email: string;
   }
   