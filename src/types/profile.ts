// types/profile.ts
export interface CompanyProfile {
    name: string;
    code: string;
    taxId: string;
    address: string;
    phone: string;
    email: string;
    defaultCurrency: string;
   }
   

   